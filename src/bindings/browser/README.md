# Browser Binding

Planned browser-facing binding surface over the Rust core.

This directory is reserved for browser-safe packaging, transport choices, and
runtime glue that do not depend on Node.js APIs.
